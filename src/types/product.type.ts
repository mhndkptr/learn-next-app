export type ProductType = {
  map(arg0: (product: ProductType) => import("react").JSX.Element): unknown;
  length: number;
  id: string;
  name: string;
  price: number;
  image: string;
  category: string;
};
